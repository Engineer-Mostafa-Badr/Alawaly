import 'package:flutter/material.dart';

class AllOffers extends StatelessWidget {
  const AllOffers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("صفحه عرض جميع العروض ")));
  }
}
