import 'package:flutter/material.dart';

class AllNews extends StatelessWidget {
  const AllNews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("صفحه عرض جميع  الاخبار ")));
  }
}
